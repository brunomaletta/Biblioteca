name: Deploy Hugo site to Pages

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      HUGO_VERSION: 0.124.0

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: ${{ env.HUGO_VERSION }}
          extended: true

      - name: Install Node.js dependencies
        run: |
          cd website
          if [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ]; then
            npm ci
          fi

      - name: Get Hugo modules
        run: hugo mod get --source website

      - name: Read baseURL from config
        run: |
          echo "HUGO_BASE_URL=$(grep -Po '(?<=^baseURL = ")[^"]*' website/hugo.toml)" >> $GITHUB_ENV

      - name: Build site
        env:
          HUGO_ENVIRONMENT: production
          HUGO_ENV: production
          HUGO_BASE_URL: ${{ env.HUGO_BASE_URL }}
        run: hugo --gc --minify --source website --baseURL "${{ env.HUGO_BASE_URL }}/"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: website/public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
